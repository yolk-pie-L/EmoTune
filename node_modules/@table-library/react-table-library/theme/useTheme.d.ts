import { Theme } from '@table-library/react-table-library/types/theme';
declare const zipThemes: (themes: Theme[]) => Theme;
declare const useTheme: (theme: Theme | Theme[]) => Theme;
export { useTheme, zipThemes };
